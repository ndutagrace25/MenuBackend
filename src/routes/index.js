const categories = require("./categories");
const dishes = require("./dishes");
const sms = require('./sendSms');
const uploads = require('./upload');

module.exports = { categories, dishes, sms, uploads};
